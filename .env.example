# 邮箱(M2)。密码只放在部署机的 .env 里,永远不进仓库。
MAILBOX=sales@example.com
IMAP_HOST=imap.example.com
IMAP_PORT=993
IMAP_USER=sales@example.com
IMAP_PASSWORD=
# 收件与已发送文件夹名。已发送留空则不收;常见名:Sent / "Sent Items" / "已发送"
IMAP_INBOX=INBOX
IMAP_SENT=
# 每隔多少秒收一次
POLL_SECONDS=60
DB_PATH=data/mail2leads.sqlite3
# 前端构建产物;serve 时托管
WEB_DIST=web/dist
