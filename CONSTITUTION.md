# mail2leads 宪法

mail2leads 把 sales@ 收到的询盘邮件读成销售线索,人确认、人回复。

九条。每条写明为什么和怎么守——守不住的条文不是宪法,是愿望。
修宪单独一次提交,只改这一个文件,并写明否决了什么、为什么。

## 第一条 语义归模型,裁决归代码

语言、意图、分类、摘要、优先级这类开放问题交给版本化的模型任务;
权限、幂等、数字核对、发不发这类必须确定的事由代码裁决。

- 为什么:这是「以 AI 为地基」的实际含义——不用关键词森林模拟模型,也不让模型裁决权限。
- 怎么守:新增一条判断语义的 if,要在 ADR 里证明它是稳定、可穷举的约束,否则改任务合同或评测样本。

## 第二条 对客户说话的只能是人

系统里不存在未经人明确授权的对外发送路径。即时回复仍须人点击并使用一次性令牌；
2026-09-23 用户明确要求的开发信序列，允许人在启用前确认固定收件人、六封正文版本
以及首封后第 7、14、28、60、90 天的时间表，后台只执行这份已保存授权。
改收件人、改正文或扩展时间表必须重新授权；任何回复、退订、硬退信、人工暂停或
发送结果不确定都停止后续发送。未经批准的导入名单和草稿不具备发送权限。

- 为什么:自动回复错一次的代价是客户收到一封胡话;摘要错一次只是业务员多看两眼原文。
- 怎么守:即时发送仍要求界面会话的一次性令牌；序列发送仅消费已持久化、绑定完整内容的人工授权。测试覆盖未授权任务、重复执行、停发条件、发送不确定和精确时间表。

## 第三条 原文不可变,派生物有署名

收到的邮件(头、正文、附件)落库后永不修改、永不删除;
每个 AI 输出旁边写着哪个模型、哪个任务版本、什么时候、基于哪封原文。

- 为什么:原文是唯一的事实来源;派生物可以重算、可以丢,但必须能追溯。
- 怎么守:原文表只允许 INSERT(触发器拒绝 UPDATE 和 DELETE);派生表的四个署名字段 NOT NULL,界面组件没有署名不渲染。

## 第四条 模型说的每个数字都要能回到原文

摘要、提取、草稿里引用的数字、型号、价格、日期,由确定性代码在原文里核对;
对不上的,界面标红并把内容压到警告之下。

- 为什么:外贸场景最贵的错误是编一个数量或价格。这条不交给模型自觉。
- 怎么守:核对器是纯函数,有攻击测试;模型输出不经过核对器不能落库。

## 第五条 建议不是事实

模型提出的线索信息(公司、数量、意向、优先级)是建议;人确认后才写进线索的事实字段。

- 为什么:线索是要拿去做生意的,错的线索比没有线索更贵。
- 怎么守:`lead_suggestion` 与 `lead` 是两张表;写 `lead` 的接口要求一个人的用户 ID,后台任务没有。

## 第六条 失败必须显形

模型没有给出合规输出时,界面显示「这封没有读数」,绝不显示空白或占位冒充结果。

- 为什么:最坏的结局不是失败,是看起来像成功的沉默——没人会去核对一封「看过了」的信。
- 怎么守:任务接口的返回类型只有 `Result | Failure` 两种;界面对 `Failure` 有专门的渲染,测试断言它出现。

## 第七条 评测先于提示词

每个模型任务先有评测集和基线分数,改动后分数不能倒退;没有评测集的任务不上线。

- 为什么:顺序反了,「调好了」就只是「看着顺眼」。
- 怎么守:`evals/<task>/` 不存在则该任务无法注册;评测脚本的退出码进 CI,幻觉率大于 0 即红。

## 第八条 外物只借不靠

别人做得好的直接拿来用:抄进我们的树、带署名、变成我们的。
但不 import 它的包、不跟它的发布节奏、不让它的数据模型当地基。
硬件同理:模型后端是适配器,一个环境变量切换。
别的系统可以依赖 mail2leads 的接口,反过来不行。

- 为什么:自己做也做不过人家,还死赖着不用,何必。真正要防的只有依赖:靠别人的更新和补丁活着,哪天它改了方向,我们跟着改。Spark 同理——它换了、坏了、被别的项目占了,这里改一行配置继续跑。
- 怎么守:适配器目录之外不出现任何主机名或别的仓库的名字(守卫 grep);依赖清单里只有库、没有产品;抄来的文件头写来源和许可证,`THIRD_PARTY.md` 与文件头由守卫核对。

## 第九条 好看是功能

表现层只由设计令牌和数据渲染;字面色值、字号、间距不出现在组件里。

- 为什么:业务员一天看几十封,排版就是效率;令牌是让「换一次皮」不变成「改一遍组件」的唯一办法。
- 怎么守:守卫扫描 `web/`,令牌文件之外出现十六进制色值即失败;Tailwind 任意值 `[…]` 一律禁止;`pages/` 只许布局类。
